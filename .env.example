
# Copy to .env (for local only) and adjust
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/insurdb
# Optional: alternative for quick local tests (no Postgres)
# DATABASE_URL=sqlite:///local.db
